# ── LangChain stack ──────────────────────────────────────────────────────────
langchain>=0.2.16,<0.3.0
langchain-community>=0.2.16,<0.3.0
langchain-openai>=0.1.23,<0.2.0
langchain-groq>=0.1.9,<0.2.0
langchain-chroma>=0.1.4,<0.2.0
langchain-core>=0.2.38,<0.3.0
langchain-text-splitters>=0.2.0,<0.3.0

# ── Vector store (thin HTTP client — no Rust/C++ compilation) ─────────────────
chromadb-client>=0.5.0

# ── Embeddings (ONNX-based — no PyTorch, no tokenizers, no Rust) ─────────────
fastembed>=0.3.0

# ── BM25 keyword search ───────────────────────────────────────────────────────
rank-bm25>=0.2.2

# ── Document loaders ─────────────────────────────────────────────────────────
pypdf>=4.3.1
python-docx>=1.1.2
docx2txt>=0.8

# ── Core utilities ────────────────────────────────────────────────────────────
pydantic>=2.8.2
python-dotenv>=1.0.1
tiktoken>=0.7.0

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit>=1.37.0
